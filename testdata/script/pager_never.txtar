# --paging=never writes directly to stdout, even when STRIPES_PAGER is set.
env STRIPES_PAGER=stub-pager
env STUB_PAGER_LOG=$WORK/.pager.log

exec stripes --color=never --paging=never input.json
! exists $WORK/.pager.log

-- input.json --
{"hello":"world"}
