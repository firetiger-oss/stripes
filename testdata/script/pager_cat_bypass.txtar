# `-p cat` with --paging=always acts as a no-op pager: same output as direct.
exec stripes --color=never --paging=always -p cat input.json
cmp stdout expected.txt

-- input.json --
{"hello":"world"}
-- expected.txt --
{
  "hello": "world"
}
