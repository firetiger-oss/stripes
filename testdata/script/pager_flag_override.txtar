# -p flag overrides the PAGER env var.
env PAGER=should-not-run
env STUB_PAGER_LOG=$WORK/.pager.log

exec stripes --color=never --paging=always -p stub-pager input.json
exists $WORK/.pager.log
grep -count=1 '^argv: .*stub-pager$' $WORK/.pager.log

-- input.json --
{"hello":"world"}
